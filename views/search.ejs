<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search — MicroSocial</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/style/global.css">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="text-white bg-zinc-900 min-h-screen">

  <%- include("partials/header", { user }) %>

  <main class="w-full max-w-[700px] mx-auto px-4 py-8">
    <h1 class="text-center font-extrabold text-3xl mb-8">Search Users</h1>

    <form action="/search" method="get" class="flex gap-3 mb-10">
      <input type="text" name="q" value="<%= query || '' %>" placeholder="Search users..."
             class="flex-1 bg-zinc-800 border border-zinc-700 rounded-xl px-4 py-3 outline-none focus:ring-2 focus:ring-blue-500 text-lg placeholder-zinc-500"/>
      <button type="submit" class="bg-blue-600 hover:bg-blue-700 rounded-xl text-white font-semibold px-5 py-3">Search</button>
    </form>

    <% if (!results || results.length === 0) { %>
      <p class="text-center text-zinc-400 text-lg">No users found.</p>
    <% } else { %>
      <div class="flex flex-col gap-6">
        <% results.forEach(u => { %>
          <a href="/user/<%= u._id %>" class="flex items-center gap-4 bg-zinc-800/30 border border-white/6 rounded-xl p-4 hover:bg-zinc-800/50 transition">
            <img src="<%= u.profile || '/profile/defaultProfile.png' %>" class="w-16 h-16 sm:w-20 sm:h-20 rounded-full object-cover border border-zinc-700" alt="user"/>
            <div class="result-name font-semibold text-xl sm:text-2xl"><%= u.name %></div>
          </a>
        <% }) %>
      </div>
    <% } %>
  </main>

</body>
</html>
